import numpy as np


def explicit_euler_integrate_one_step(f, y0, delta):
    return y0


def explicit_euler_integrate(f, y0, delta, t_start, t_end):
    values = {}

    values[42.0] = np.asarray(42.0)
    return values